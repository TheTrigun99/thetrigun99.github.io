---
title: "DDPM"
categories: Deep Learning
tags: [DL]
toc: true
math: true
lang: fr
page_id: DDPM
permalink: /posts/DDPM/
---

## Préambule

Le code relié à ce post ce trouve sur mon [github](https://github.com/TheTrigun99/ProjectsDL).

Montons le niveau de ce blog avec du generative modeling qui consistue une grosse partie de mes prochains travaux. Plus particulièrement, on va se concentrer sur de la diffusion en utilisant tout d'abord des notions probabilistes (Denoising Diffusion Probabilistic Models). 
Ce post sera probablement update ou d'autres parties seront faites.

Je ne compte pas faire un énieme post en dérivant les équations et développer le principe, car plein d'articles géniaux ont été fait: [lilan-weng](https://lilianweng.github.io/posts/2021-07-11-diffusion-models/).
Je compte surtout parler de ce que je n'ai pas trouver dans ces articles, les points importants que j'ai remarqué et comment coder cela durement. 

Ce post sera actualisé, car je compte approfondir en continuant les grandes lignes des modèles de diffusion.

## Introduction

Les modèles de diffusion sont des modèles génératifs devenus très importants après l’apparition des DDPM en 2020, en particulier parce qu’ils ont progressivement surpassé les GANs sur de nombreuses tâches de génération d’images.

## DDPM : l'idée de base

Commençons par introduire le modèle basique, [DDPM](https://arxiv.org/abs/2006.11239). 

Les modèles de diffusion sont des modèles génératifs qui cherchent à apprendre une distribution de données $p_{\text{data}}$, par exemple une distribution d'image.

L'idée de DDPM est de ne pas apprendre directement cette distribution. À la place, on définit d'abord un processus de bruitage progressif : on part d'une image réelle $x_0 \sim p_{\text{data}}$, puis on ajoute petit à petit du bruit gaussien jusqu'à obtenir une variable $x_T$ proche d'un bruit pur $\mathcal{N}(0,I)$.

Le modèle apprend ensuite le processus inverse : partir d'un bruit pur et retirer progressivement le bruit pour revenir vers une image réaliste. Autrement dit, au lieu d'apprendre directement une distribution d'images très complexe, on apprend une suite de petits pas de débruitage.

C'est cette idée qui rend le problème plus simple : chaque étape $x_t \rightarrow x_{t-1}$ est locale, beaucoup plus facile à apprendre que la transformation complète $\mathcal{N}(0,I) \rightarrow p_{\text{data}}$.

Le processus forward est défini par :

$$
q(x_t \mid x_{t-1})
=
\mathcal{N}
\left(
x_t ;
\sqrt{1-\beta_t}x_{t-1},
\beta_t I
\right).
$$

On pose :

$$
\alpha_t = 1-\beta_t,
\qquad
\bar{\alpha}_t = \prod_{s=1}^t \alpha_s.
$$

Une propriété très utile des gaussiennes (appelée "the nice property" par L.Weng)  est que l'on peut écrire directement $x_t$ en fonction de $x_0$ :

$$
x_t
=
\sqrt{\bar{\alpha}_t}x_0
+
\sqrt{1-\bar{\alpha}_t}\varepsilon,
\qquad
\varepsilon \sim \mathcal{N}(0,I).
$$

Le réseau est alors entraîné à prédire le bruit ajouté :

$$
\mathcal{L}_{\text{simple}}
=
\mathbb{E}_{t,x_0,\varepsilon}
\left[
\left\|
\varepsilon
-
\varepsilon_\theta(x_t,t)
\right\|^2
\right].
$$

Un point très important est que la solution pour $\varepsilon_\theta(x_t,t)$ que doit donner le modèle correspond à $\mathbb{E} \left[ \varepsilon | x_t, t   \right]$.


À la génération, on part de $x_T \sim \mathcal{N}(0,I)$, puis on applique progressivement le modèle appris pour obtenir $x_0$.


## Le setup

MNIST est le dataset classique et que je pensais inutile, car trop simple, néanmoins si l'on s'en sert bien sa simplicité est très utile.
Utiliser MNIST et entraîner un modèle sur peu de données permet de faire des expériences rapidement (quelques minutes maximum pour entraîner) quand l'on a accès uniquement au GPU de Colab et donc de comprendre rapidement les modèles et les moduler.
Il est aussi intéréssant de concevoir des modèles assez puissant pour que ça marche, mais pas assez puissant pour voir les limites du modèle et ses échecs.

## What I learned from building DDPM

Coder le modèle de diffusion n'est pas difficile, mais comprendre les choix qui ont été fait, leur importance et voir ceux-ci à l'oeuvre est plus compliqué.

### U-net architecture is strong
Le Deep Learning nous sert dans notre cas donc à prédire le bruit ajouté à l'instant t $eps(x_t, t)$ et j'ai commencé simplement en voulant utiliser un MLP (neuronet dans mon code).
J'ai voulu optimiser pendant 1h30 mon MLP pour faire descendre la loss, mais malheuresement même sur un dataset comme MNIST un MLP basique n'a aucune chance: il doit apprendre from scratch la structure des images (des pixels proches sont vraisemblablement corrélés) ce que font des convolutions (biais inductif spatial) et doit apprendre à générer des images à des niveaux de bruits différents où il doit donner des détails fins à petit t et la structure global à grand t... 
Le MLP a donc trop de choses à apprendre tout seul et sans un gros modèle avec énormément de paramètres et de training, c'est très compliqué.  


Je me suis donc mis à faire un U-net ce qui était bien plus costaud à coder, mais aussi très formateur.   
Le U-net était à 6 années lumières du MLP et demande peu de paramètres par rapport à ce qu'il propose.   
Mon U-net comprend quelques améliorations récentes telles que le rescaling par 1/sqrt(2) ou encore l'adaGN ce qui donne de meilleurs résultats.    
La raison réside dans l'architecture ddu U-net. Il apprend les détails fins grâce aux skip connections et la structure global en faisant du Downsampling/bottle neck au contrario d'un MLP.  
De plus, un U-net comprend la spatialité des images comme il est consistué convolutions: **biais inductif spatial** (je n'ai pas ajouté de transformers dans ce 1er U-net) et est donc très efficace sur des images et le problème de diffusion tout en demandant peu de compute (par rapport à un MLP équivalent).   

Here's some sampling of the diffusion model from some random noise:
![samplingst](/assets/img/ddpm/outputc8.png){: width="60%" }

Since we train our model with only 1k5 images and with a low capacity model, i'm pretty satisfied of the result.



### The time is interesting

The time looks innocent and we do not really think about it at first (other than "yeah the model can know with time if he has to add more noise or less"), but 2 questions came up to me.  
What happens if I remove time in my U-net? Time stills appear in the sampling with the beta-schedule, but I can remove it from my U-net context.
Note that time is encoded with a sinuosidal embedding, we have to give tensors to the model.  
If we remove time as context from the model, the model take some input and try to denoise it apparently without knowing how noised is the input or not easily:  
We can infer that a model with enough data and capacity (not really efficient and the idea of this project) could grasp the distribution with respect to time, but it looks unstable (you could imagine some a bit noised image due to random conditions of life and the model would take it as a noised input not as a distribution input).  

DDPM still isn't easy to train on small dataset given we want to see the limits of the model and having efficient compute, but that's where you learn the most, I guess.  

You have too remember that biais accumulate during the sampling and thus with less data or less epoch the results changes a lot.
Here's the difference between :    

1k5 images, U-netdim=32, no time, 500 epoch  
![outputwotime8](/assets/img/ddpm/output6d.png){: width="60%" } 
The model learns the mean of all the numbers which gives this blurry shape or sometimes he can't get the right direction in space giving only noise.

700 epoch  
![outputwotime7](/assets/img/ddpm/output7d.png){: width="60%" } 

My second question was "how the model learns across time?  

That's where the interesting part about the "simple loss" given in the DDPM paper:  
If we keep the initial ELBOW loss, the small noises are less ponderated and the bigger one have more weight, thus the model will train more on bigger noises and less on small noises where he's got to be cautious and precise to get the details.  
Doing a uniform loss counters this a bit by equilibrating the learning across time.
You have access to the loss for each timestep in my code.


## DDIM

Avec les expériences précédentes, on voit à quel le modèle est sensible sur comment on gère les timesteps et le temps, néanmoins une limitation du DDPM, c'est le sampling où les timesteps justement jouent le rôle principal.  
Une hypothèse principal du modèle est que l'on a des gaussiennes comme posterior avec des petits timpesteps, donc plus on fait de timesteps plus cette hypothèse est vérifiée.  
Pour l'instant, le modèle apprend à débruiter petit à petit, mais ça veut dire que quand on sample, on doit faire 300 compute avec le modèle (j'ai choisis 300 timesteps) ce qui demande du temps quand on veut sample plusieurs images.  

A plus grande échelle et sur des images de haute résolution (pas 28x28 comme MNIST), la différence de temps de sampling est abyssale entre un DDPM et un GAN: “For example, it takes around 20 hours to sample 50k images of size 32 × 32 from a DDPM, but less than a minute to do so from a GAN on an Nvidia 2080 Ti GPU.”  [Song et al.](https://arxiv.org/pdf/2010.02502)   

Avant de passer au [DDIM](https://arxiv.org/abs/2010.02502), j'ai essayer de simplement sauter des étapes dans le sampling DDPM classique au lieu d'aller de 1 en 1, mais ce n'est pas concluant.

LE main insight de [DDIM](https://arxiv.org/abs/2010.02502), c'est que la loss ne dépend pas de la loi jointe q(x1,...,xT |x_0), mais uniquement des q(x_t| x_0).  
Dans le reverse process, on va de 1 en 1 pour suivre la chaîne de Markov inverse, car c'est ce que nous dit de faire la loi jointe q(x1,...,xT | x_0) = q(x_T|x0) produit des q(xt-1 |x_t,x_0), mais la loss est en fait bien moins contraignante.  
C'est là où la propriété des gaussiennes "the nice property" est magique, on a un lien direct entre x_t et x_0. Sans ces factorisations de gaussiennes, on serait contraint de suivre la loi inverse q(x_t-1 | x_t).  
L'idée maintenant de DDIM, c'est de voir que du coup, on peut choisir un process différent avec une loi jointe différente, mais qui a les mêmes marginale q(x_t | x_0) ce qui donne exactement la même loss.  
Ils utilisent ainsi un sampling déterministe (plus gaussien comme dans le DDPM où on rajoute du bruiy à chaque timestep) et vont utiliser moins de pas, on fait T/S steps au lieu de T.  
Cela n'était pas concluant en DDPM à cause du bruit ajouté (stochasticité).  
On ne peut néanmoins toujours pas passer de T à 0 directement, notre modèle découpe l'information petit à petit même avec DDIM.  

Ce qui est néanmoins intéréssant de noter, c'est que DDIM nécéssite un meilleur modèle pour sampler aussi bien que DDPM. Le bruit ajouté en DDPM lors du sampling peut corriger l'erreur du modèle ce qui freine l'accumulation d'erreur tout au long du sampling.  
L'erreur va elle se propager dans le DDIM comme c'est devenu déterministe et que l'on a plus cette stochasticité.  

Avec assez de données, j'arrive à diviser par 5x le nombre de steps sans trop perte de qualité:

![outputddim](/assets/img/ddpm/outputddim.png){: width="60%" }
Unet=64, 4k images, 700 epoch, S=5

L'output est néanmoins moins variés que le DDPM classique et c'est dû à la nature déterministe du sampling et au fait que le modèle reste entraîné sur peu de données:

![outputddim2](/assets/img/ddpm/outputddpmim.png){: width="60%" }
même modèle mais DDPM

## Classifier-free guidance

On sait maintenant apprendre une distribution et sampler, mais on sample une image random. Nous on aimerait conditionner et choisir ce que l'on sample.  
Pour ça, on fait de la classifier guidance.  
Géométriquement, c'est assez simple si on adopte la vue du score (grad log p(x_0)):  
Le score nous permet de nous diriger dans l'espace jusqu'à la distribution.  
Notre loss DDPM revient en fait à apprendre le score ou du moins c'est équivalent (cf Score matching Y.Song).  
Cela vient de la formule de Tweedie (ajouter formule de Tweedie !!)  
En utilisant Bayes, on a directement que grad log p(x_0 |y) = grad logp(x_0) + grad log p (y| x0).  
Et c'est le deuxième terme qui correspond à un classifier qui nous permet d'aller vers un endroit choisi de la distribution.  
Maintenant, on ne va pas entraîner un classifier externe ici mais entraîner la moitié du temps le modèle à avoir le conditionnement comme données durant l'entraînement et l'autre moitié sans conditionnement.  
Ainsi, on va pouvoir ajouter les deux contributions et nous diriger correctement dans l'espace.  

![CFG 1](/assets/img/ddpm/outputcfg.png){: width="60%" } 
Conditionnement sur 1 dans l'espace
















##



